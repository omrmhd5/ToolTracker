services:
  postgres:
    image: postgres:16
    container_name: tooltracker-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: tooltracker
      POSTGRES_PASSWORD: tooltracker
      POSTGRES_DB: tooltracker
    volumes:
      - tooltracker_pgdata:/var/lib/postgresql/data

volumes:
  tooltracker_pgdata:
